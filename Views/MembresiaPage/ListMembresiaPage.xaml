<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Name="Root"
    x:Class="GymPos.Views.MembresiaPage.ListMembresiaPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GymPos.Views.MembresiaPage"
    xmlns:models="using:GymPos.Models"
    xmlns:conv="using:GymPos.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Margin="20" RowSpacing="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- ================= NOTIFICACIÓN ================= -->
        <Border
            x:Name="NotificationToastBorder"
            Grid.Row="0"
            Visibility="Collapsed"
            CornerRadius="12"
            Padding="20,16"
            Margin="0,0,0,10"
            Background="{ThemeResource SolidBackgroundFillColorSecondaryBrush}"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="1">

            <Grid ColumnSpacing="16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Grid
                    x:Name="IconContainer"
                    Grid.Column="0"
                    Width="40"
                    Height="40"
                    CornerRadius="8"
                    Background="#0078D4">

                    <FontIcon
                        x:Name="NotificationIcon"
                        Glyph="&#xE946;"
                        FontSize="20"
                        Foreground="White"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"/>
                </Grid>

                <StackPanel
                    Grid.Column="1"
                    VerticalAlignment="Center"
                    Spacing="4">

                    <TextBlock
                        x:Name="TitleTextBlock"
                        Text="Notificación"
                        FontSize="16"
                        FontWeight="SemiBold"
                        Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>

                    <TextBlock
                        x:Name="MessageTextBlock"
                        Text="Mensaje"
                        FontSize="13"
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                </StackPanel>

                <Button
                    Grid.Column="2"
                    Padding="8"
                    Background="Transparent"
                    VerticalAlignment="Top"
                    Click="CloseToast_Click">

                    <FontIcon Glyph="&#xE71B;" FontSize="14"/>
                </Button>
            </Grid>
        </Border>

        <!-- ================= ENCABEZADO ================= -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock
                Grid.Column="0"
                Text="Membresías"
                FontSize="20"
                FontWeight="SemiBold"
                VerticalAlignment="Center"
                Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"/>

            <Button
                Grid.Column="1"
                Command="{x:Bind ViewModel.AddCommand}"
                Padding="16,8"
                CornerRadius="8"
                Background="{ThemeResource AccentAAFillColorDefaultBrush}">

                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon Glyph="&#xE710;" FontSize="14" Foreground="White"/>
                    <TextBlock Text="Agregar membresía" Foreground="White"/>
                </StackPanel>
            </Button>
        </Grid>

        <!-- ================= LISTA DE MEMBRESÍAS ================= -->
        <GridView
            Grid.Row="2"
            ItemsSource="{x:Bind ViewModel.MembresiaList}"
            SelectionMode="None"
            
            IsItemClickEnabled="False"
            Padding="0"
            Margin="0,8,0,0">

            <GridView.ItemTemplate>
                <DataTemplate x:DataType="models:Membresia">
                    <Border
                        Width="260"
                        Height="200"
                        Padding="20"
                        CornerRadius="16"
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource AccentAAFillColorDefaultBrush}"
                        BorderThickness="1">

                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <!-- Nombre -->
                            <TextBlock
                                Text="{Binding Nombre}"
                                FontSize="18"
                                FontWeight="SemiBold"
                                Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>

                            <!-- Sesiones -->
                            <StackPanel
                                Grid.Row="1"
                                Orientation="Horizontal"
                                Spacing="6"
                                Margin="0,4,0,0">

                                <FontIcon
                                    Glyph="&#xE8C7;"
                                    FontSize="13"
                                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>

                                <TextBlock
                                    FontSize="13"
                                    Foreground="{ThemeResource TextFillColorSecondaryBrush}">

                                    <Run Text="{Binding Sesiones}"/>
                                    <Run Text=" sesiones"/>
                                </TextBlock>
                            </StackPanel>

                            <!-- Precio -->
                            <TextBlock
                                Grid.Row="2"
                                FontSize="22"
                                FontWeight="Bold"
                                VerticalAlignment="Center"
                                Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}">

                                <Run Text="S/ "/>
                                <Run Text="{Binding Precio}"/>
                            </TextBlock>

                            <!-- Botón editar -->
                            <Button
                                Grid.Row="3"
                                Click="OnEditMembresiaClick"
                                HorizontalAlignment="Right"
                                Padding="10,5"
                                CornerRadius="6"
                                Background="Transparent"
                                BorderBrush="Red"
                                BorderThickness="1">

                                <StackPanel Orientation="Horizontal" Spacing="6">
                                    <FontIcon
                                        Glyph="&#xE70F;"
                                        FontSize="12"
                                        Foreground="Red"/>

                                    <TextBlock
                                        Text="Editar"
                                        FontSize="12"
                                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                </StackPanel>
                            </Button>
                        </Grid>
                    </Border>
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>

        <!-- ================= DIALOGOS ================= -->
        <ContentDialog
            x:Name="CreateMembresiaDialog"
            Title="Agregar membresía"
            Width="600"
            CornerRadius="16"
            Padding="0"
            PrimaryButtonText="Guardar"
            CloseButtonText="Cancelar"
            PrimaryButtonClick="CreateMembresiaDialog_PrimaryButtonClick">
            <ContentDialog.Resources>
                <SolidColorBrush
                    x:Key="ContentDialogButtonForeground"
                    Color="#2563EB"/>
            </ContentDialog.Resources>
            <local:CreateMembresiaPage/>
        </ContentDialog>

        <ContentDialog
            x:Name="EditMembresiaDialog"
            Title="Editar membresía"
            Width="600"
            CornerRadius="16"
            Padding="0"
            PrimaryButtonText="Guardar"
            CloseButtonText="Cancelar"
            PrimaryButtonClick="EditMembresiaDialog_PrimaryButtonClick">
            <local:EditMembresiaPage/>
        </ContentDialog>
    </Grid>
</Page>
<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="GymPos.Views.MembresiaPage.CreateMembresiaPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GymPos.Views.MembresiaPage"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Width="400">
        <StackPanel Spacing="8" Padding="8">
            <StackPanel Orientation="Horizontal" Spacing="8">
                <Border Width="44" Height="44" CornerRadius="22" Background="#4F46E5">
                    <FontIcon Glyph="&#xE8C7;" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <StackPanel>
                    <TextBlock Text="Nueva membresía" FontSize="18" FontWeight="SemiBold"/>
                    <TextBlock Text="Registrar membresía en el sistema" Opacity="0.6" FontSize="12"/>
                </StackPanel>
            </StackPanel>

            <!-- Nombre -->
            <StackPanel Spacing="2">
                <TextBlock Text="Nombre" FontWeight="SemiBold"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="42"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Border CornerRadius="10,0,0,10">
                        <FontIcon Glyph="&#xE77B;" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <TextBox Grid.Column="1" Text="{Binding Nombre, Mode=TwoWay}" PlaceholderText="Ingrese nombre" Height="40"/>
                </Grid>
            </StackPanel>

            <!-- Sesiones -->
            <StackPanel Spacing="2">
                <TextBlock Text="Sesiones" FontWeight="SemiBold"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="42"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Border CornerRadius="10,0,0,10">
                        <FontIcon Glyph="&#xE791;" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <TextBox Grid.Column="1" Text="{Binding Sesiones, Mode=TwoWay}" PlaceholderText="Ingrese sesiones" Height="40"/>
                </Grid>
            </StackPanel>

            <!-- Precio -->
            <StackPanel Spacing="2">
                <TextBlock Text="Precio" FontWeight="SemiBold"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="42"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Border CornerRadius="10,0,0,10">
                        <FontIcon Glyph="&#xEAFD;" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <TextBox Grid.Column="1" Text="{Binding PrecioDouble, Mode=TwoWay}" PlaceholderText="Ingrese precio" Height="40"/>
                </Grid>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
